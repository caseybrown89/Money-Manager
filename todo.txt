In no particular order:

13) Throw exception from DB if name of Person is already taken on new entry, handle exception gracefully
15) Add Modal check for deleting entry
16) Add option to email balance with summary of entries
17) Creating user settings section, where user can set their Display Name, amount of entries to include in email, etc
    - Possibly open first time application is launched.